package ImpClass;

@FunctionalInterface
public interface FunctionalInterfaceDemo {

    // Functional Interface should have only single "abstract" method.
    // Functional Interface can have many "default" and "static" methods.

    int operation(int a, int b);

}
